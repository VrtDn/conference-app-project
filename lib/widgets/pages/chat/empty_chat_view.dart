import 'package:flutter/cupertino.dart';

class EmptyChatView extends StatelessWidget {
  const EmptyChatView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Open any chat in people page'),
    );
  }
}
